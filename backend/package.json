{
  "name": "@flytripvisa/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required'",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:setup": "wrangler d1 execute flytripvisa-ai-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}